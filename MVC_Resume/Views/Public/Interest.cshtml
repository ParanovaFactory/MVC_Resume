@using MVC_Resume.Models.Entity
@model List<Tbl_Interests>

<div class="my-auto">
    <h2 class="mb-5">Interests</h2>
    @foreach (var item in Model)
    {
        <p>@item.InterestDescriptions</p>
    }
</div>